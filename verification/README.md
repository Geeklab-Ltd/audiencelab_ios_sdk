# Verification evidence

This directory stores machine-checkable evidence produced by:

```bash
python3 scripts/verify_ios_integration_contract.py
```

Primary artifact:

- `evidence/latest.json`

The harness does not require Xcode or network access and must never write raw API keys.
